---
description: 禁止手改 OpenAPI 生成的 src/shared/api/admin；只通过 npm run openapi 更新
alwaysApply: true
---

# Admin OpenAPI 生成物（不可手改）

通用入口：`docs/skills/ai-code/SKILL.md`。

规则正文已收拢到：

- `docs/skills/project-rules/SKILL.md` 的「Admin OpenAPI 生成物」

Cursor 必须保持本规则 alwaysApply；不要在 `.cursor/rules` 里复制正文，避免与 Codex / Claude Code 规则漂移。
