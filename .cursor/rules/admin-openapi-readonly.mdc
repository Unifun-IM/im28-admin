---
description: 禁止手改 OpenAPI 生成的 src/shared/api/admin；只通过 npm run openapi 更新
alwaysApply: true
---

# Admin OpenAPI 生成物（不可手改）

`src/shared/api/admin/**`（含 `auth.ts`、`users.ts`、`typings.d.ts`、`index.ts` 等）全部由 `npm run openapi` 从远程 OpenAPI 生成。

## 必须遵守

1. **禁止**对该目录下任何文件做编辑、格式化「顺手改」、重命名、删减注释或改函数签名。
2. 需要新接口 / 改契约：更新远端 OpenAPI 文档后执行 `npm run openapi` 重新生成。
3. 业务适配（字段映射、默认参数、组合调用）写在 `features` / `pages` / 手写 `@shared/api/biz` 或新建非 `admin/` 封装层，**不要**改生成文件。
4. 手写请求基础设施仅限：`src/shared/api/request.ts`（及测试）；勿把业务逻辑塞进 `admin/`。

## 反例 / 正例

```ts
// ❌ 在 admin/auth.ts 里加封装或改返回类型
// ❌ 为过 lint 去改 admin/typings.d.ts

// ✅ import { postV1AdminAuthLogin } from '@shared/api/admin/auth'
// ✅ 在 login feature 里处理 next_step / 存 token
// ✅ 文档变更后：npm run openapi
```
