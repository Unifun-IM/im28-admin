---
description: 优先 Arco 标准组件；Figma 约束交互细节；非标准组件像素级布局
alwaysApply: true
---

# UI 实现优先级（全局基本准则）

1. **优先采用 Arco Design 标准组件**（及 Pro 惯用法）  
   - `Form`、`Grid`、`Space`、`Card`、`Table`、`Button`、`Select`…  
   - 能用组件 + props 解决的，不用手写布局或 Tailwind 重做

2. **Figma 约束交互细节**（落在标准组件上）  
   - 状态、反馈、间距观感、色 / 圆角 / 字号等  
   - 用 `get_design_context` + 截图对照；差异用 `use-*` / props 补齐  
   - **不要**为贴稿拆掉 Grid 等标准结构  
   - **Figma 只读**：禁止 `use_figma` / `generate_figma_design` 等任何写入；只读对照，代码对齐稿面

3. **非标准组件强制像素级布局**  
   - 无 Arco 对应物的自建 UI（如 page-tabs 凹角、侧栏特殊造型等）  
   - 必须图层数值 + 截图双验证，像素级还原

4. **Tailwind 最后** — 仅补自建装饰；`preflight: false`；禁止替代 Form/Grid/Table

细节见 `AGENTS.md`。
