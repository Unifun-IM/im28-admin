---
description: 对接接口不改交互；后端暂不支持的筛选/操作也先保留 UI
alwaysApply: true
---

# 对接接口不改交互（全局基本准则）

接 OpenAPI / 真实接口时，**以现有 Figma 与页面交互为准**，不要为迁就后端能力而简化 UI。

## 必须遵守

1. **禁止**因「接口没有该字段 / 暂不支持」而删除或隐藏筛选条件、工具栏按钮、Tab、批量操作、弹窗步骤等既有交互。
2. 请求体：契约里有的字段正常传；**没有的条件先留在 Form / 控件上**（可不进 body），待 OpenAPI 补齐后再接线——不要先拆掉入口。
3. 整页无契约：用 `ApiNotReady` 占位，**仍保留路由与菜单**；不要改成「精简版假交互」或重新引入 mock。
4. 字段名跟 `AdminAPI`；Form/Table **不做映射层**。交互保留 ≠ 擅自改生成物（见 `admin-openapi-readonly`）。

## 反例 / 正例

```ts
// ❌ 接口没有 online_status，就从筛选区删掉「在线状态」
// ❌ 批量搜索接口未就绪，就去掉「批量搜索」入口
// ✅ 保留筛选项与校验/重置；fetch 时只附带契约已有字段
// ✅ 在注释或 TODO 标明「待 OpenAPI：xxx」
```

细节见 `AGENTS.md`「全局基本准则」§5 与「API」。
