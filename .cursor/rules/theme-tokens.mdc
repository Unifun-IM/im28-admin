---
description: 颜色必须使用 theme-tokens 配色表，适配亮/暗色
alwaysApply: true
---

# 主题色 / 暗亮色适配

项目同时支持亮色与暗色。Figma 稿中的颜色应对齐已导出的配色表，**禁止**在业务样式里写死浅色 hex / `rgba(0,0,0,*)`。

## 权威来源

- 运行时：`src/app/styles/theme-tokens.less`（`body` / `body[arco-theme='dark']`）
- 设计源：`docs/Light.tokens.json`、`docs/Dark.tokens.json`
- 切换：`changeTheme` → `body[arco-theme='dark']`；品牌阶由 `applyThemeColor` 写入

## 必须用 token（常用映射）

| Figma / 稿面常见值 | Token |
|---|---|
| 页面底 `#F7F8FA` | `var(--color-bg-1)` |
| 卡片/表格/弹层底 `#FFF` | `var(--color-bg-2)` / `var(--color-bg-popup)` |
| 主/次/弱文案 | `--color-text-1` / `-2` / `-3` |
| 分割线 `rgba(0,0,0,0.08)` | **`var(--color-border-2)`**（暗色自动变白 8%） |
| 遮罩 `rgba(0,0,0,0.4)` | **`var(--color-mask-1)`** |
| 填充灰 `#F2F3F5` / `#E5E6EB` | `--color-fill-2` / `-3` |
| 品牌紫 `#635CFF`（勿用旧紫 `#7B61FF`） | `rgb(var(--primary-6))` |
| 品牌浅底 12% | `var(--color-primary-light)` |
| 成功/警告/危险 | `rgb(var(--success-6))` 等；浅底用 `--color-*-light` |

Tailwind：优先 `text-arco-text-*`、`bg-arco-bg-*`、`border-arco-border-2`、`text-primary-6`。

## 写法约定

1. **属性值本体必须是 `var(--…)` / `rgb(var(--…))`**；fallback 浅色 hex 仅作兜底，可写可不写。
2. 优先语义 token，少写 `body[arco-theme='dark']` 分支；仅图表渐变、特殊阴影等 token 不够时再局部覆盖。
3. 对照 Figma 时，把稿面 `text/1`、`border/2`、`bg/页面背景`、`primary/6` 映射到上表，不要抄导出代码里的绝对色。
4. 新增 Modal / Drawer 遮罩一律 `var(--color-mask-1)`。

## 反例

```less
/* ❌ */
border: 1px solid rgba(0, 0, 0, 0.08);
color: #7b61ff;
background: #fff;

/* ✅ */
border: 1px solid var(--color-border-2);
color: rgb(var(--primary-6));
background: var(--color-bg-2);
```
