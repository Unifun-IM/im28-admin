---
description: 严格限定每次指令边界；禁止自行扩大到同类问题或全仓追溯
alwaysApply: true
---

# 严格限定指令边界（全局基本准则）

每一次用户指令只做其**明确点名**的范围，不做「顺手扩修」。

## 必须遵守

1. **范围以当条指令为准** — 点名的页面 / 组件 / 文件 / 现象才改；未点名的模块一律不动。
2. **禁止自行扩面** — 不得因「同类问题」「历史对接搞坏了」「一并优化」去批量改其它 Drawer / Modal / 列表，除非用户写明「批量 / 全部 / 同类一并 / 全仓追溯」等。
3. **发现相关债只汇报** — 实现中撞到邻近问题：在回复里用一两句列出，**停下等下一指令**；不要直接改。
4. **不借清理名义扩 scope** — 不顺手重构、不批量格式化、不「升级」无关依赖或文案。
5. **多文件仅限完成该指令所必需** — locale / 样式 / 调用方等最小连带可以改；「看起来也该修」的不算必需。

## 反例 / 正例

```text
// ❌ 用户说「用户详情交互坏了」→ 顺手恢复群详情、查聊天、全仓 ApiNotReady
// ❌ 用户说「修一下筛选样式」→ 重写整页列表与对接逻辑
// ✅ 用户说「用户详情」→ 只改 UserDetailDrawer（及必要 locale/样式）
// ✅ 发现群详情也是 stub → 回复里提一句，等用户再说
```

细节见 `AGENTS.md`「全局基本准则」§6 与「改动边界」。
