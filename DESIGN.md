# Project Design Specification

本文件是当前项目由 `PROJECT.md`、已确认设计需求和框架 `design-system` 归一得到的设计规范。AI code 生成或修改可见 UI 前读取本文件；通用尺度、组件和 CSS 实现规则仍以对应 skill 为准。

## 设计目标

- 服务高频后台操作，优先保证信息扫描、比较、查询和连续操作效率。
- 视觉保持安静、克制和专业，内容层级清楚，不使用营销页式编排或无业务意义的装饰。
- PC 页面是信息架构、字段与业务能力的设计基准；窄屏采用 H5 常见交互重组流程，不按比例压缩桌面布局。

## 视觉方向

- 当前项目没有额外品牌视觉覆盖，沿用 Arco Design Pro、项目主题 token 和公共组件的浅色 / 暗色契约。
- 中性色承担页面与信息层级，主色只表达选中、焦点和主要动作，成功、警告、危险使用对应语义色。
- 页面背景、业务面、描边、圆角、字号和间距继承 `design-system`；业务页面不建立平行 token 或局部视觉体系。

## 体验方向

- 标准后台页面保持列表优先、操作路径直接，并覆盖加载、空态、错误、最长文案和双语状态。
- 支持手机、平板和桌面；窄屏使用单列渐进流程、遮罩导航、全屏详情层和明确返回路径，保持主要操作连续可达。
- 响应式可以改变信息的承载层级与导航方式，但必须保留 PC 基准中的字段语义、权限、操作能力和数据顺序。

## 项目设计差异

当前项目没有相对公共 `design-system` 的额外品牌、密度或媒体风格覆盖。IM 实体详情保持统一 Drawer 视觉；关系数据使用子 Drawer，聊天记录使用只读主从视图，并在窄屏切换为可返回的单面板流程。

未在本文件中定义的视觉行为继承 `docs/skills/design-system/SKILL.md`；具体任务的设计来源优先级也由该 skill 维护。
