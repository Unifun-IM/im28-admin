# Project Context

> 角色：产品经理。定义产品为什么做、为谁做、做什么，以及业务边界与验收目标。

本文件是当前项目的产品决策上下文，供 Codex、Claude Code、Cursor 等 AI code 工具共同读取。它由 `project-context` skill 从文字需求和相关 PRD 增量维护，只记录已确认、跨页面且后续任务仍有效的项目级事实。

本文件对产品定位、范围、信息架构、业务术语和跨页面流程负责；不决定视觉方案、CSS 实现、技术选型或接口契约。视觉目标交给 `DESIGN.md`，工程落地由 `AGENTS.md` 和专项 skills 负责。

## 产品定位

- 项目名称：`im-admin`。
- 项目形态：基于 `admin-scaffold` 最新结构建设的 IM 管理后台，同时保留基础后台能力、公共业务组件和 AI 标准页面生成机制。
- 主要使用者：执行用户、群组、会话、系统配置和风控等高频工作的运营与后台管理人员，以及持续维护该后台的前端工程师。
- 核心目标：围绕生成的 Admin OpenAPI 提供可查询、可追溯、可配置且响应式可用的 IM 运营管理能力，并让后续页面继续遵循统一生成契约。

## 当前范围

- 基础能力包括登录、后台壳层、主题与语言、首页看板、后台账号、角色、系统参数和系统操作日志。
- IM 业务范围包括用户查询、黑白名单、用户日志、群组查询与设置、单聊 / 群聊会话查询、会话设置和只读聊天记录。
- 资产运营范围包括充值地址查询与状态管理、提现订单查询与审核；资金敏感操作必须完成当前管理员的二步验证。
- 平台配置范围包括资产场景 Banner、客户端版本和平台条款；系统参数仍属于系统管理，不并入平台配置。
- 风控当前提供 IP 黑名单；交易域保留红包记录、配置和详情代码，但当前不在侧栏展示。
- 用户查询和用户日志复用同一个用户详情；群组及成员使用统一详情视觉，关系数据通过新的子 Drawer 承载。

## 信息架构

- 一级导航顺序为：首页看板、用户、群组、会话、资产、平台配置、系统、风控；隐藏的交易模块不影响可见导航顺序，系统始终位于业务模块之后。
- 用户：用户管理（用户查询、黑名单、白名单）、用户日志。
- 群组：群组查询、群组设置。
- 会话：会话查询（用户会话查询、群组会话查询）、会话设置。
- 资产：充值地址、提现订单。
- 平台配置：Banner 管理、客户端版本、平台条款。
- 系统：后台账号管理、角色管理、系统参数设置、系统操作日志。
- 风控：IP 黑名单。

## 稳定项目决策

- AI 页面生成是项目能力的一部分，生成结果与现有 IM 页面使用同一套路由、组件、接口和主题基础设施。
- 列表中的实体入口复用该实体唯一详情；详情中的好友、群聊、成员等关系钻取由独立子 Drawer 承载，不在主详情中增加无业务必要的中间 Tab。
- 会话查询按用户会话和群组会话拆分二级路由，聊天记录保持只读，不引入发送消息等运营端写操作。
