# Project Context

> 角色：产品经理。定义产品为什么做、为谁做、做什么，以及业务边界与验收目标。

本文件是当前项目的产品决策上下文，供 Codex、Claude Code、Cursor 等 AI code 工具共同读取。它由 `project-context` skill 从文字需求和相关 PRD 增量维护，只记录已确认、跨页面且后续任务仍有效的项目级事实。

本文件对产品定位、范围、信息架构、业务术语和跨页面流程负责；不决定视觉方案、CSS 实现、技术选型或接口契约。视觉目标交给 `DESIGN.md`，工程落地由 `AGENTS.md` 和专项 skills 负责。

## 产品定位

- 项目名称：`admin-scaffold`。
- 项目形态：包含基础后台能力、公共业务组件和 AI 标准页面生成机制的管理后台框架，不是空白脚手架。
- 主要使用者：在框架上开发业务后台的前端工程师，以及使用派生后台执行查询、配置、审核和风控等高频任务的运营人员。
- 核心目标：让派生项目基于生成的 Admin OpenAPI、明确需求和统一设计契约，增量生成可维护、可响应式使用的标准后台页面。

## 当前范围

- 框架提供登录、后台壳层、主题与语言、基础工作台、账号、角色、系统参数、操作日志等基础能力。
- 页面生成覆盖菜单与路由、筛选列表、详情 Drawer、关系 Drawer、操作记录、设置页和 locale。
- 业务域、导航树和字段闭合集合由派生项目的明确需求、完整 Figma 或 PRD 补充；本模板不预设具体业务信息架构。

## 信息架构

- 模板提供看板、系统管理和系统参数等基础入口。
- 派生项目的业务导航、模块顺序和统一术语由其已确认需求定义；本模板不预设具体业务菜单。

## 稳定项目决策

- AI 页面生成是框架能力的一部分，生成结果应与手写页面使用同一套路由、组件、接口和主题基础设施。
- 派生项目保留基础通用能力，同时允许按自身产品范围扩展业务页面。
