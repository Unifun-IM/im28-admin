# API 根地址（Admin 网关）
# 本地仅 Mock 时可改为 /
VITE_API_BASE_URL=https://im-api-gateway.djftech.app

# OpenAPI YAML（npm run openapi）
# 可为远程 URL，或相对仓库根目录的本地路径（如 docs/openapi.yaml）
OPENAPI_YAML_URL=https://im-api-gateway.djftech.app/docs/admin/openapi.yaml
